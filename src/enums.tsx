export enum ItemType {
  Folder = "folder",
  File = "file",
  FolderPlaceholder = "folder-placeholder",
}

export enum EventType {
  Click = "click",
  KeyDown = "keydown",
}

export enum Key {
  Escape = "Escape",
  Enter = "Enter",
}
